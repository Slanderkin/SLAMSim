#pragma once

#ifndef GUI_H
#define GUI_H

/*
void buttonSetup(std::vector<Button>& buttonList, Robot robot, World world, bool& finalBool, sf::Font font) {


	bool bools[3] = { world.drawWorld,robot.scan.doGaussian,finalBool };
	float sizes[3][2] = { {100.f,50.f},{100.f,50.f},{100.f,50.f} };
	float pos[3][2] = { {10.f,10.f},{120.f,10.f},{230.f,10.f} };
	std::string textStr[3] = { "Draw World","Gaussian", "Draw Lines" };


	
	sf::Color color = sf::Color::Blue;
	for (int i = 0; i < 3; i++) {
		sf::Text text(textStr[i],font,24);
		text.setFillColor(sf::Color::White);
		Button button(sizes[i][0], sizes[i][1], pos[i][0], pos[i][1], color, bools[i], text);
		buttonList.push_back(button);
	}
}

*/


#endif




